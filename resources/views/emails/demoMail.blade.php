<!DOCTYPE html>
<html>
<head>
    <title>ItsolutionStuff.com</title>
</head>
<body>
    <h1>{{ $mailData['title'] }}</h1>
    <p>{{ $mailData['username'] }}</p>
    <p>{{ $mailData['email'] }}</p>
    <p>{{ $mailData['phone'] }}</p>

    <p>{{ $mailData['body'] }}</p>

    <p>Thank you</p>
</body>
</html>
